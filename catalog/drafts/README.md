# Catalog drafts

Human-review-only staging area for proposed catalog entries (for example output from `python3 scripts/qk import-candidates`).

- **Never** merge these drafts into `catalog/templates.yaml` automatically.
- Promote selected candidates in a separate PR after license, PHI, and fitness review.
